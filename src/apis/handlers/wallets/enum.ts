export enum WalletType {
	MAIN = "MAIN",
	SPOT = "SPOT",
	FUTURES = "FUTURES",
}

export enum PaymentCategory {
	CRYPTO = "Crypto",
}

export enum PaymentOperation {
	DEPOSIT = "Deposit",
	WITHDRAWAL = "Withdrawal",
}
